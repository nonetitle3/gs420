# GS420 AI Security

Implemented: allow-listed uploads, size limits, safe filenames/UUID storage, bounded request fields, restricted calculator, AST checks, execution time/output limits, temporary execution directories, environment secrets, request/error logging.

Production: replace wildcard CORS with exact origins, enable authentication/authorization and rate limiting, use HTTPS, secret storage, monitoring, and isolated container/VM execution for untrusted code. Never automatically execute model-generated shell commands.
