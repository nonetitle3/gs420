import React,{useEffect,useState}from"react";import{chat,plan,searchDocuments,models}from"./services/api";
const tabs=["Chat","Coding","Vision","Image","Video","Voice","Agents","Documents","Memory","Settings"];
export default function App(){const[tab,setTab]=useState("Chat"),[input,setInput]=useState(""),[messages,setMessages]=useState([]),[busy,setBusy]=useState(false),[modelList,setModelList]=useState([]);
useEffect(()=>{models().then(setModelList).catch(()=>{})},[]);
async function send(){if(!input.trim()||busy)return;const q=input.trim();setInput("");setMessages(m=>[...m,{role:"user",text:q}]);setBusy(true);try{let r=tab==="Agents"?await plan(q):tab==="Documents"?await searchDocuments(q):await chat(q);setMessages(m=>[...m,{role:"assistant",text:JSON.stringify(r,null,2)}])}catch(e){setMessages(m=>[...m,{role:"system",text:"Request failed: "+e.message}])}finally{setBusy(false)}}
return <main><header><div><h1>GS420 AI</h1><p>Model: Auto · Memory: Enabled · {modelList.length} registered models</p></div><span>● Ready</span></header><nav>{tabs.map(x=><button className={tab===x?"active":""} onClick={()=>setTab(x)} key={x}>{x}</button>)}</nav><section className="panel"><h2>{tab}</h2>{tab==="Settings"&&<pre>{JSON.stringify(modelList,null,2)}</pre>}<div className="messages">{messages.map((m,i)=><article className={m.role} key={i}>{m.text}</article>)}</div><textarea value={input}onChange={e=>setInput(e.target.value)} placeholder={tab==="Documents"?"Search documents…":"Ask anything…"}/><button onClick={send} disabled={busy}>{busy?"Processing…":"Send"}</button></section></main>}
