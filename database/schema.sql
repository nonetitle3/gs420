CREATE TABLE conversations(id INTEGER PRIMARY KEY,session_id TEXT,role TEXT,content TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP);CREATE TABLE memories(id INTEGER PRIMARY KEY,kind TEXT,content TEXT,created_at DATETIME DEFAULT CURRENT_TIMESTAMP);CREATE TABLE preferences(key TEXT PRIMARY KEY,value TEXT);