# GS420 AI Android

The repository contains a Capacitor scaffold in `mobile/capacitor`.

```bash
cd mobile/capacitor
npm install
npx cap add android
npx cap sync
npx cap open android
```

Build the APK with Android Studio. Heavy inference should remain on a backend/Colab/GPU service. Do not hard-code secrets.
