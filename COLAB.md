# GS420 AI Google Colab

Select a GPU runtime when available, clone the repo, then run `python scripts/colab_setup.py` and `python scripts/colab_check.py`. Configure `GS420_MODEL_ID` and optional specialist IDs. Run `python app.py` for Gradio. Use Colab Secrets for private tokens. GPU availability and runtime persistence are not guaranteed.
